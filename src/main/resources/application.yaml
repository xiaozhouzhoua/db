spring:
  datasource:
    url: jdbc:mysql://localhost:3306/vsdb?autoReconnect=true&characterEncoding=utf-8&useSSL=false&allowMultiQueries=true&rewriteBatchedStatements=true
    username: root
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 100

  redis:
    host: localhost
    port: 6379
